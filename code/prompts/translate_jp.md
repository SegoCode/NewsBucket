**GOAL**:
You are a professional translator. Translate the JSON array below from Spanish to Japanese. Translate ONLY "title" and "summary" fields. Keep "source", "count", and the overall JSON structure identical. Return ONLY a valid JSON array (no markdown, no extra text).

**CRITICAL RULES**:
- Translate title and summary faithfully, keeping the journalistic tone
- Do NOT modify "source", "count", or any other field
- Return ONLY valid JSON, no markdown, no extra text
